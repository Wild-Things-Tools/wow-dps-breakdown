# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
web/dist/
*.local

# Pipeline scratch: raw simc output is huge and reproducible, only the
# distilled dataset under web/public/data is committed.
.work/
simc-bin/
*.simc.out
*.raw.json

# Fight probe output and its response cache. The cache is what makes re-running
# the extraction free, but it is raw API payloads: large, and not ours to commit.
fight-probe/

# Editors / OS
.DS_Store
.idea/
.vscode/

# Local screenshot helper (see CLAUDE.md); not part of the app
web/shot.mjs

# Git worktrees created for parallel agent runs. Tooling state, not project content.
.claude/worktrees/

# Screenshot output from the local helper above.
web/shots*/
